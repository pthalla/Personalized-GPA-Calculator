<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity" >

    <item android:id="@+id/menu_item_delete_course"
        android:icon="@drawable/ic_action_remove"
        android:title="Remove course"
        android:showAsAction="ifRoom|withText"/>

    <item android:id="@+id/menu_item_new_course"
        android:icon="@drawable/ic_action_new"
        android:title="New course"
        android:showAsAction="ifRoom|withText"/>

    <item android:id="@+id/switch_button"
        android:title="Switch GPA scale (A- = 4.0)"
        android:showAsAction="never" />
</menu>